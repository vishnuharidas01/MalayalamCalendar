import {
    StyleSheet
  } from 'react-native';

const styles = StyleSheet.create ({
    monthContainer: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        marginVertical: 10,
        marginHorizontal: 5,
      }
});

export default styles;